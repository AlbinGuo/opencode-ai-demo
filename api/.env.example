# Database Configuration (choose one)
# Option 1: Individual settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=demo
DB_USER=postgres
DB_PASSWORD=admin

# Option 2: DATABASE_URL (for Railway, Render, etc.)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Application Settings
ENVIRONMENT=development  # Set to "production" in production
FRONTEND_URL=http://localhost:3000

# Gitee OAuth2 (get from https://gitee.com/oauth/applications)
GITEE_CLIENT_ID=your_gitee_client_id
GITEE_CLIENT_SECRET=your_gitee_client_secret
GITEE_REDIRECT_URI=http://localhost:8002/auth/gitee/callback

# JWT Secret (change in production!)
JWT_SECRET_KEY=your-super-secret-key-change-in-production

# Boss Zhipin Spider Cookie (optional, for better crawling)
# BOSS_COOKIE=your_boss_zhipin_cookie
