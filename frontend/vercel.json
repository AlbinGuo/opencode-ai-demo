{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vue",
  "env": {
    "VITE_API_URL": "@api-url"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
